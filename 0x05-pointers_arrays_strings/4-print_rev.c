#include <stdio.h>
#include <string.h>
#include "main.h"

/**
 * print_rev - function to print reverse of a string
 * @s: character to reverse
 * Return: void
 */
void print_rev(char *s)
{
	return (strrev(s))
}
