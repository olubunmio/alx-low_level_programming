#include <stdio.h>
#include "main.h"
#include <string.h>
/**
 * _strlen - get lenght of a string
 * strlen - standard library to get lenght of a string
 * @s: string pointer
 * Return: integer
 */
int _strlen(char *s)
{
	return (strlen(s));
}
