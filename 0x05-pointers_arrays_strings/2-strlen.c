#include <stdio.h>
#include "main.h"
#include <string.h>
/**
 * str_len - get lenght of a string
 * _strlen - standard library to get lenght of a string
 * @s: string pointer
 * Return: integer
 */
int _strlen(char *s)
{
	return (strlen(s));
}
