#include "main.h"

/**
 * _sqrt_recursion - square root of a number
 * @n: integer
 *
 * Return: integer
 */
int is_prime_number(int n)
{
	if (n / 1 == n || n / n == 1)
	{
		return (1);
	}
	return (0);
}
